# TODO

## ✅ Выполнено

### Backend
- [x] Структура проекта: `backend/` и `frontend/`
- [x] FastAPI приложение с lifespan и CORS middleware
- [x] Асинхронный цикл опроса оборудования (`poll_devices_loop`)
- [x] OLT-клиент на Netmiko с ThreadPoolExecutor (`olt_client.py`)
- [x] SNMP-клиент на PySNMP asyncio (`snmp_client.py`)
- [x] Парсинг вывода `show gpon onu state` (форматы A и B)
- [x] Автоматическое получение Description для проблемных ONU
- [x] Парсинг `sysDescr` для вендоров ZTE, SNR, D-Link, Eltex
- [x] WebSocket менеджер с broadcast (`ws_manager.py`)
- [x] REST endpoint `/api/data` для первичной загрузки
- [x] WebSocket endpoint `/ws` для live-обновлений
- [x] Раздельные конфиги: `inventory.py` (список устройств), `settings.py` (параметры)

### Frontend
- [x] Svelte + Vite + Tailwind CSS
- [x] Подключение к WebSocket и REST API
- [x] Карточки OLT/Switch с индикаторами статуса
- [x] Раскрывающиеся порты с таблицей ONU
- [x] Фильтры по статусу (LOS, DyingGasp, Offline, working)
- [x] Поиск по имени интерфейса и номеру договора
- [x] Глобальный фильтр «Только аварийные»
- [x] Пагинация портов (16 на страницу) и ONU (50 на страницу)
- [x] Экспорт порта в CSV
- [x] Таймер обратного отсчёта до следующего опроса
- [x] Тёмная/светлая тема
- [x] Toast-уведомления об обновлениях и ошибках
- [x] Звуковые алерты при авариях (Web Audio API)
- [x] Метрики: всего / в работе / LOS / DyingGasp

---

## 🔧 В работе

- [ ] Docker Compose для развёртывания всей системы

---

## 📋 Запланировано

### Backend
- [ ] Логирование LOS-событий в JSON Lines файл (`events.jsonl`)
  - Фиксировать `contract`, `onu_id`, `olt_ip`, `started_at` при переходе в LOS
  - Дописывать `ended_at` и `duration_sec` при восстановлении
- [ ] Детектор массовых аварий — алерт если на PON-порту упало >30% ONU одновременно
- [ ] Подсчёт суммарного времени недоступности ONU за сутки/неделю (для SLA)
- [ ] REST API для внешних систем — эндпоинты для интеграции с другими инструментами мониторинга
- [ ] REST endpoint для получения истории по договору (`/api/history/{contract}`)
- [ ] Telegram-бот — уведомление в чат NOC при массовой аварии на порту

### Frontend
- [ ] Живой счётчик времени в LOS для каждой строки таблицы (`2ч 14м`)
- [ ] История отключений по договору — раскрывается по клику, показывает даты и длительность каждого инцидента
- [ ] Счётчик падений на строке договора (`⚠️ 5 за 7 дней`)
- [ ] Цветовая тепловая карта по OLT — визуальное отображение severity каждого устройства с первого взгляда
- [ ] Сортировка портов по количеству LOS
- [ ] Уведомления в браузер (Notification API) — работают даже при свёрнутой вкладке
- [ ] Настройки интерфейса (интервал опроса, пороги алертов)
- [ ] График динамики аварий (Chart.js / D3)
