# TODO (План разработки)

## ✅ Выполнено

### Backend & Архитектура
- [x] Структура проекта: разделение на `backend/` и `frontend/`
- [x] FastAPI приложение с подсистемой lifespan, CORS middleware и асинхронным циклом опроса
- [x] OLT-клиент на Netmiko с поддержкой платформ ZTE ZXA10 C300/C320 и C600/C650 (`olt_client.py`)
- [x] SNMP-клиент на PySNMP asyncio для коммутаторов доступа (`snmp_client.py`)
- [x] Один глобальный изолированный `SnmpEngine` — предотвращает зависание Event Loop при массовом опросе
- [x] Парсинг состояния `show gpon onu state` и автоматический опрос `detail-info` / `User-Defined-Rid`
- [x] Разделение аппаратно-физического обрыва линии (`LOS`) и проблем на стороне абонента (`LOSi`)
- [x] Анализ реальных причин падения с фильтрацией битых дат прошивки (`0000-00-00`)
- [x] Парсинг системных описаний `sysDescr` для вендоров ZTE, SNR, D-Link, Eltex
- [x] Переход от JSON-файлов к реляционной СУБД SQLite (`noc_history.db`)
- [x] Потокобезопасная запись в SQLite через механизм глобальной блокировки транзакций (`threading.Lock`)
- [x] Детектор массовых аварий (>60% на OLT-порту или >30% коммутаторов в локации)
- [x] Серверный аналитический движок «Ночного аудита» (`audit_engine.py`) для суточных и сменных отчётов (17:00–09:00)
- [x] WebSocket менеджер броадкастов (`ws_manager.py`) и REST API (`/api/data`, `/api/stats/daily`, `/api/history`, `/api/audit/month`)
- [x] Запуск принудительного обновления сети (`/api/update/force`) с управлением через `asyncio.Event`
- [x] Изолированная конфигурация: `inventory.py/md` (списки устройств), `settings.py` (параметры опроса)

### Безопасность & Авторизация (v4.0.0)
- [x] Модуль безопасности `utils/auth.py` с поддержкой Bearer JWT-токенов и валидацией сессий
- [x] Динамическая генерация суточных паролей доступа операторов смены (`ДДММГГГГ`)
- [x] Защита от брутфорса: автоматический бан IP-адреса на 24 часа после 3 неверных попыток ввода
- [x] Скрытая админ-панель безопасности (`/sessions`) и защищенные эндпоинты (`/api/admin/security`, `/session/kill`, `/ip/ban`, `/ip/unban`)
- [x] Модуль клиентских гардов `security.js`: отключение контекстного меню, клавиш DevTools (F12, Ctrl+Shift+I/J/C, Ctrl+U/S) и цикличный анти-отладчик (`debugger`)
- [x] Модальные окна авторизации `LoginModal.svelte` и `AdminLoginModal.svelte`

### Frontend & UI
- [x] Svelte 4 + Vite + Tailwind CSS с реактивной тёмной/светлой темой (с сохранением в LocalStorage)
- [x] Предзагрузочный кэш состояния сети (`noc_cached_network_data`) для моментального рендеринга (0 сек задержки)
- [x] Подключение к WebSocket с авто-реконнектом и статусным диодом подключения
- [x] Интерактивный плавающий виджет фонового опроса OLT (`PollingHud.svelte`) с индикатором 0–100%
- [x] Декомпозиция логики на хранилища Svelte (`networkStore.js`, `themeStore.js`, `notificationStore.js`)
- [x] Модульная структура микро-компонентов (`SwitchCard`, `SwitchSidebar`, `SwitchToolbar`, `GponPortCard`, `GponToolbar`, `GponOnuTable`, `DashboardAnomalies`, `NightAuditSwitchItem`)
- [x] Радужная анимированная строка поиска (Rainbow Border) по IP, интерфейсам, договорам и моделям
- [x] Умный авто-прыжок по номеру договора с автоматическим раскрытием OLT, платы и нужной вкладки
- [x] Сортировка плат OLT (сначала аварии, затем по названию/нагрузке)
- [x] Фильтры "Только LOS" и "Только LOSi" с скрытием заведомо "здоровых" локаций
- [x] Вывод индивидуальных счетчиков `LOS` и `LOSi` в меню, на карточках и платах
- [x] Экспорт таблицы абонентов выбранного порта в файл CSV
- [x] Таймер обратного отсчёта до следующего авто-опроса
- [x] Живые счетчики длительности простоя для каждого упавшего узла
- [x] График динамики потерь (Chart.js) на главном дашборде
- [x] Система браузерных Push-уведомлений (Notification API) о падении/восстановлении хостов
- [x] Модальное окно истории инцидентов за 30 дней с кластеризацией дребезга линий (Flapping)
- [x] Календарь «Ночного аудита» с цветовой отметкой проблемных дней и выбором смен

### Инфраструктура & Docker
- [x] Docker Compose — контейнеризация полного стека (Backend + Frontend + Nginx)
- [x] Nginx Reverse Proxy — единая точка входа (порт 80), проксирование API и WebSocket без CORS-проблем
- [x] Изолированная внутренняя сеть Docker Bridge
- [x] Volume-маппинг базы данных SQLite (`./backend/data:/app/backend/data`)
