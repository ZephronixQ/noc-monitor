# TODO

## ✅ Выполнено

### Backend
- [x] Структура проекта: `backend/` и `frontend/`
- [x] FastAPI приложение с lifespan и CORS middleware
- [x] Асинхронный цикл опроса оборудования (`poll_devices_loop`)
- [x] OLT-клиент на Netmiko с поддержкой ZTE C300/C320 и C600 (`olt_client.py`)
- [x] SNMP-клиент на PySNMP asyncio (`snmp_client.py`)
- [x] Парсинг вывода `show gpon onu state`
- [x] Автоматическое получение Description/User-Defined-Rid для проблемных ONU
- [x] Разделение статусов `LOS` (обрыв линии) и `LOSi` (проблема у абонента)
- [x] Парсинг реальной причины падения через `detail-info` с игнорированием `0000-00-00`
- [x] Парсинг `sysDescr` для вендоров ZTE, SNR, D-Link, Eltex
- [x] WebSocket менеджер с broadcast (`ws_manager.py`)
- [x] REST endpoint `/api/data` для первичной загрузки
- [x] WebSocket endpoint `/ws` для live-обновлений
- [x] Раздельные конфиги: `inventory.md/py` (список устройств), `settings.py` (параметры)
- [x] Переход от плоских JSON-файлов истории к реляционной базе данных SQLite (`noc_history.db`)
- [x] Обеспечение потокобезопасности при записи в СУБД через систему блокировок (`threading.Lock`)
- [x] Детектор массовых аварий (>60% на OLT-порту или >30% коммутаторов в локации)
- [x] Эндпоинты статистики за сутки (`/api/stats/daily`) и истории (`/api/history`)
- [x] Один глобальный `SnmpEngine` — устранено зависание Event Loop при опросе коммутаторов
- [x] Эндпоинт принудительного обновления OLT (`/api/update/force`) и управление таймером через `asyncio.Event`

### Frontend
- [x] Svelte + Vite + Tailwind CSS
- [x] Подключение к WebSocket с авто-реконнектом и визуальным индикатором статуса
- [x] Карточки OLT/Switch с индикаторами статуса и цветовой тепловой картой (severity)
- [x] Сортировка портов OLT (сначала массовые аварии, затем по убыванию количества проблем)
- [x] Раскрывающиеся порты с таблицей ONU и фильтрами (Все, онлайн, LOS, LOSi, DyingGasp, Offline)
- [x] Локальная пагинация и ручная сортировка таблиц абонентов
- [x] Поиск по имени интерфейса, IP и номеру договора
- [x] Глобальный авто-прыжок по договору (поиск с автоматическим раскрытием OLT, порта и нужной вкладки)
- [x] Фильтр "Только LOS" и "Только LOSi" в UI
- [x] Вывод счетчиков `LOSi` в дашборд, карточки портов и боковое меню
- [x] Экспорт порта в CSV
- [x] Таймер обратного отсчёта до следующего опроса
- [x] Тёмная/светлая тема (с сохранением в LocalStorage)
- [x] Декомпозиция логики на реактивные хранилища Svelte (`stores/`)
- [x] Разделение интерфейса на модульные папки (`components/common`, `components/gpon`, `components/switches`, `components/dashboard`, `components/audit`)
- [x] Уведомления системного уровня в браузере (Notification API)
- [x] Живой счётчик времени простоя для каждого аварийного элемента
- [x] График динамики аварий (Chart.js) на главном дашборде
- [x] Модальное окно истории подключений (Timeline падений за 30 дней с группировкой флуда в кластеры)
- [x] Интерактивный календарь истории («Ночной аудит») с фильтрацией инцидентов по сменам

### Инфраструктура
- [x] Docker Compose — одной командой разворачивает весь стек (Backend + Frontend)
- [x] Проксирование API и WebSocket через контейнер Nginx (Reverse Proxy)
- [x] Volume-маппинг для SQLite базы данных (`./backend/data:/app/backend/data`)
